import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  mapPageImageContainer: {
    alignItems: 'center',
    paddingBottom: 23,
    paddingTop: 200,
    marginBottom: 23,
  },
});
export default styles;
